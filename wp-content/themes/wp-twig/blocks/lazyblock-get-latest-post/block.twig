<h2>{{category.name}}</h2>
<div>{{category.category_description}}</div>

{% for post in posts %}
  <div class="col-md-4">
    <h3>
      <a href="{{post.link}}">
        {{post.title}}
      </a>
    </h3>
    {{ post.content }}
  </div>
{% endfor %}